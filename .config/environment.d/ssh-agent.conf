# Static default for systemd user processes (Claude Code, services).
# Priority matches ~/.shell/exports.sh: 1Password → GPG → ~/.ssh/agent/* fallback.
# exports.sh overrides this if the socket doesn't exist at shell startup.
SSH_AUTH_SOCK=${HOME}/.1password/agent.sock
