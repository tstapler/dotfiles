-- Sample first migration — replace with your real schema.
-- Generated by `sqlx migrate add init` conventions (timestamped filename
-- in the real project; this scaffold uses a placeholder name).
create table if not exists healthcheck (
    id bigserial primary key,
    checked_at timestamptz not null default now()
);
