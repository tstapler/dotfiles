---
description: Break changes into a reviewable stack of PRs using git-machete. Runs Evaluate → Plan → Execute → Ship stages.
---

Run the git-stacked-prs skill for the current branch.

$ARGUMENTS are passed directly to the skill (optional — leave empty to start a new stack from the current branch).

Invoke the git-stacked-prs skill now.
