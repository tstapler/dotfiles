---
all:
  children:
    bastion:
      hosts:
        bastion-1:
          ansible_host: "{{ lookup('community.general.onepassword', 'Escutcheon/bastion-1', field='address') }}"
          ansible_user: ubuntu
          ansible_ssh_private_key_file: "~/.ssh/id_ed25519_bastion"
          ansible_port: 22  # bootstrap port; after role runs, use ssh_bastion_port
