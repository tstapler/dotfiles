---
- name: Check Nix installed
  stat:
    path: /nix/var/nix/profiles/default/bin/nix
  register: nix_bin
  tags: [nix]

- name: Warn if Nix not installed
  debug:
    msg: "Nix not installed — run bootstrap/run.sh to install it (requires sudo)"
  when: not nix_bin.stat.exists
  tags: [nix]

- name: Verify Nix version
  command: /nix/var/nix/profiles/default/bin/nix --version
  register: nix_version
  changed_when: false
  when: nix_bin.stat.exists
  tags: [nix]

- name: Show Nix version
  debug:
    msg: "{{ nix_version.stdout }}"
  when: nix_bin.stat.exists
  tags: [nix]
