# LiteLLM Proxy Environment Variables
# Copy to .env and customize

# AWS Configuration (for Bedrock fallback)
AWS_PROFILE=Sandbox.AdministratorAccess
AWS_REGION=us-west-2

# Database (for LiteLLM features like health check disable)
# PostgreSQL runs in Docker on port 47432
DATABASE_URL=postgresql://litellm:litellm@localhost:47432/litellm

# Optional: Postgres password (default: litellm)
# POSTGRES_PASSWORD=litellm

# Note: LITELLM_MASTER_KEY is NOT required with custom_auth
# The proxy accepts Anthropic OAuth tokens (sk-ant-oat-*) directly

# Note: ANTHROPIC_API_KEY is NOT required
# Claude Code passes its OAuth token via x-api-key header
